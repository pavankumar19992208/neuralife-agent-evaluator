version: "3.9"
services:
  api:
    build: ./api
    container_name: nle_api
    ports:
      - "8000:8000"
    environment:
      - DATA_DIR=/data
      - PYTHONUNBUFFERED=1
    volumes:
      - ./api:/app
      - ./data:/data
      - ./ui:/app/ui:ro
    restart: unless-stopped
